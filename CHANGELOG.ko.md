# 변경 이력

[English](CHANGELOG.md) · 한국어

## 미출시

- **`ss import` 가 이미 있는 파일을 읽습니다.** `.xcstrings` 파일이나 `.lproj` 들이 있는
  디렉터리를 가리키면 시트를 만듭니다. 키마다 한 행, 언어마다 한 열, 주석은 `description`
  열, 복수형은 키 접미사입니다. 지금까지는 모든 명령이 시트가 있다고 가정해서, 문자열이
  300개인 앱은 손으로 옮겨 적어야 했습니다. 변수는 `{arg1}` 로 돌아옵니다. 파일에는 변수의
  자리만 남아 있기 때문입니다.
- **Homebrew.** `brew install keenkim1202/tap/stringsmith` 로 CLI 와 `ss` 별칭이 깔리고
  `brew upgrade` 로 갱신됩니다. `xattr` 단계가 없습니다. 격리 딱지는 브라우저로 받은 파일에만
  붙기 때문입니다. formula 는 릴리스 워크플로가 갱신하므로 tap 이 릴리스보다 뒤처지지
  않습니다.
- **실패가 종료 코드로 구분됩니다.** `2` 는 설정이나 시트를 읽지 못한 경우, `3` 은 시트는
  읽었지만 내용이 검증을 통과하지 못한 경우입니다. 예전에는 전부 `1` 이라 CI 스크립트가
  "설정을 고쳐라"와 "시트를 채워라"를 구분할 수 없었습니다. 표와 CI 예시는 README 에 있습니다.
- **오프라인 경고가 캐시 나이를 함께 알립니다** (`캐시를 사용합니다 (12일 전)`).
  없으면 몇 달 전 캐시로 조용히 빌드됩니다.

## 0.2.0

아래는 전부 첫 릴리스 이후에 들어간 것입니다. 0.1.0 을 쓰고 계시다면 알아 두실 동작 변경은
하나입니다 — **Swift 이름이 충돌하면 이제 `generate` 가 멈춥니다.** 경고만 내던 이전 동작이
필요하면 `validation.failOn` 을 보세요.

### 소스

- **Google Sheets 를 URL 로.** `ss init --url …` 이 시트를 바로 읽습니다. CSV 로 내보낼
  필요가 없습니다. 마지막으로 성공한 응답을 캐시해 두므로 오프라인이라고 빌드가 멈추지 않습니다.
- **비공개 시트 로그인.** `ss auth login` 이 브라우저를 한 번 엽니다(PKCE·loopback). 내 계정으로
  열 수 있는 시트는 공유 설정을 풀지 않아도 읽힙니다. OAuth 클라이언트는 내장하지 않고 각자
  등록합니다 — `ss auth setup` 이 채워 넣을 파일을 만들어 줍니다.
- **엑셀.** `.xlsx` 를 의존성 없이 바로 읽습니다.
- **여러 탭**을 `source.tabs` 로 이어 붙입니다.

### 산출물

- **`--format`** 으로 String Catalog 와 `.strings`/`.stringsdict` 중에 고릅니다.
- **복수형.** 키 접미사로 적습니다(`cart.items.one`). 두 형식 모두 제대로 담고, 생성되는
  접근자는 `Int` 를 받는 함수 하나입니다.
- **`output.swift.path`** 로 생성 코드를 리소스 디렉터리 밖에 둘 수 있습니다.

### 검사

- **`ss validate`** 는 파일을 만들지 않고 시트만 검사합니다. CI 용 `--strict` 가 있습니다.
- **`ss drift`** 는 시트와 코드가 어긋난 키를 찾습니다 — 안 쓰는 키와, 문자열로 부르는데
  시트에 없는 키.
- **`validation.failOn`** 이 어떤 경고에서 멈출지 정합니다. 이름 충돌은 기본으로 멈춥니다.
- 새 규칙: 키 이름, 비가시 문자, CLDR 기준 복수형 범주, 그 언어에서 유난히 긴 번역.

### 고친 것

- 번역 값의 앞뒤 공백을 지우지 않습니다. 의도한 것일 수 있습니다.
- 원문 칸이 정당하게 비는 행(원문 언어에 없는 복수형)을 누락으로 보고하지 않고, 그 번역도
  변환해서 내보냅니다.
- 탭을 이어 붙였을 때 오류가 어느 탭의 행인지 짚습니다.
- `--header-row` 에 단축 옵션이 없었습니다. 자동으로 잡힌 `-h` 가 `--help` 와 충돌했기
  때문입니다. `-r` 입니다.

## 0.1.0

첫 릴리스. CSV·TSV 를 읽어 `.xcstrings` 와 타입 안전 Swift 접근자를 만들고, 변수 표기를
iOS 포맷 지정자로 바꿉니다. 결과를 확인하는 macOS 앱이 함께 있습니다.
